import App from '../App';
import MainTemplate from '../components/templates/MainTemplate';

const Root = () => {
  return (
    <MainTemplate>
      <App />
    </MainTemplate>
  );
};

export default Root;
